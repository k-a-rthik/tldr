# 7z

> उच्च संपीड़न अनुपात वाला फ़ाइल संग्रहकर्ता।  
> अधिक जानकारी: <https://manned.org/7z>।

- किसी नए या मौजूदा संग्रह में एक फ़ाइल या निर्देशिका [a]dd करें:

`7z a {{संग्रह.7z/का/पथ}} {{फ़ाइल_या_निर्देशिका/का/पथ}}`

- मौजूदा संग्रह (फ़ाइल नाम सहित) को एन्क्रिप्ट करें:

`7z a {{एन्क्रिप्टेड.7z/का/पथ}} -p{{पासवर्ड}} -mhe=on {{संग्रह.7z/का/पथ}}`

- मूल निर्देशिका संरचना को संरक्षित करते हुए संग्रह को E[x]tract करें:

`7z x {{संग्रह.7z/का/पथ}}`

- किसी विशिष्ट निर्देशिका में संग्रह को E[x]tract करें:

`7z x {{संग्रह.7z/का/पथ}} -o{{आउटपुट/का/पथ}}`

- संग्रह को `stdout` में E[x]tract करें:

`7z x {{संग्रह.7z/का/पथ}} -so`

- किसी विशिष्ट संग्रह प्रकार का उपयोग करके [a]rchive करें:

`7z a -t{{7z|bzip2|gzip|lzip|tar|zip}} {{संग्रह/का/पथ}} {{फ़ाइल_या_निर्देशिका/का/पथ}}`

- संग्रह की सामग्री [l]ist करें:

`7z l {{संग्रह.7z/का/पथ}}`

- संपीड़न स्तर निर्धारित करें (उच्च मान अधिक संपीड़न लेकिन धीमी गति दर्शाता है):

`7z a {{संग्रह.7z/का/पथ}} -mx={{0|1|3|5|7|9}} {{फ़ाइल_या_निर्देशिका/का/पथ}}`
